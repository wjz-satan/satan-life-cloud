<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.satan.life.analytics.mapper.UserSpendingAnalysisMapper">

    <select id="selectByUserIdAndDateRange" resultType="com.satan.life.analytics.entity.UserSpendingAnalysis">
        SELECT
            id,
            user_id,
            analysis_date,
            total_spending,
            category_spending,
            monthly_comparison,
            create_time,
            update_time,
            deleted
        FROM
            user_spending_analysis
        WHERE
            user_id = #{userId}
            AND analysis_date BETWEEN #{startDate} AND #{endDate}
            AND deleted = 0
        ORDER BY
            analysis_date DESC
    </select>

    <select id="selectLatestByUserId" resultType="com.satan.life.analytics.entity.UserSpendingAnalysis">
        SELECT
            id,
            user_id,
            analysis_date,
            total_spending,
            category_spending,
            monthly_comparison,
            create_time,
            update_time,
            deleted
        FROM
            user_spending_analysis
        WHERE
            user_id = #{userId}
            AND deleted = 0
        ORDER BY
            analysis_date DESC
        LIMIT 1
    </select>

</mapper>